% preamble.tex

% color support
\usepackage{xcolor}

% typography tweaks
\usepackage{microtype}               % better font protrusion and spacing
\usepackage{parskip}                 % no indent + a bit of vertical space between paras

% headers & footers
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhead{}  
\fancyfoot{}  
\fancyhead[LE,RO]{\nouppercase{\rightmark}}
\fancyfoot[CE,CO]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% code listings
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  showstringspaces=false,
  numbers=left,
  numberstyle=\tiny,
  frame=single
}

% figure/table captions
\usepackage[font=small,labelfont=bf]{caption}

% graphics
\usepackage{graphicx}
